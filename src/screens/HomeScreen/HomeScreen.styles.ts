import { createStyles, makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() =>
  createStyles({
    header: {
      paddingTop: 70,
      paddingBottom: 70,
      marginBottom: 15,
    },
  })
);
